const ProfileImage = ({image}) => {
    return (
        <div>
            <img src={image} className='profile' alt='profile' />
            <h4>hi</h4>
        </div>
    )
}

export default ProfileImage;